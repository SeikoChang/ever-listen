# ever-listen
Flutter (single UI codebase + native plugins). It gives near-native performance, strong tooling for native plugin work (AudioRecord/AVAudioEngine, WebRTC VAD native libs, foreground services/background modes), and high code reuse while keeping platform-specific native code limited to the audio/VAD/background pieces.
